#include<iostream>
#include<bits/stdc++.h>
using namespace std;

void leftonerotate(vector<int>&vec)
{
  int temp = vec[0];
  int temp2;
  for(int i=1; i<vec.size(); i++)
  { 
    vec[i-1] = vec[i];
  }
  vec[vec.size()-1] = temp;
}

void leftrotate(int arr[],int n)
{
  int temp = arr[0];
  for(int i=1; i<n; i++)
  {
    arr[i-1] = arr[i];
  }
  arr[n-1] = temp;
}
void print_Arr(int arr[], int n)
{
  for(int i=0; i<n; i++)
  {
    cout<<arr[i]<<" ";
  }
}

void print_vector(vector<int>&v)
{
  for(int i=0; i<v.size(); i++)
  {
    cout<<v[i]<<" ";
  }
}

int main()
{
  vector<int>v = {1,2,3,4,5};
  int arr[] = {5,6,7,8,9};
  int n = sizeof(arr)/sizeof(arr[0]);
  print_vector(v);
  leftonerotate(v);
  cout<<endl;
  print_vector(v);
  cout<<endl;
  print_Arr(arr,n);
  cout<<endl;
  leftrotate(arr,n);
  print_Arr(arr,n);
}